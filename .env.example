# Kopieer dit bestand naar ".env" (zelfde map als docker-compose.yml) en
# vul de echte waarden in. ".env" staat in .gitignore en wordt dus NOOIT
# meegenomen door git -- zo komt het databasewachtwoord niet in de
# (publieke) repository terecht.
DB_USER=phpserver
DB_PASS=verander_dit_wachtwoord
